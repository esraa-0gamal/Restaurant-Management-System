@model RestaurantSystem.Models.Dish

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-info text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">تفاصيل الطبق</h4>
            <div>
                <a asp-action="Edit" asp-route-id="@Model.DishId" class="btn btn-warning btn-sm">تعديل</a>
                <a asp-action="Dishes" class="btn btn-light btn-sm">العودة للقائمة</a>
            </div>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4 text-center mb-3">
                    <img src="@(Model.ImageUrl ?? "/images/default-dish.png")"
                         alt="@Model.Name"
                         class="img-fluid rounded shadow-sm border"
                         style="max-height: 250px; width: 100%; object-fit: cover;" />
                </div>
                <div class="col-md-8">
                    <table class="table table-bordered">
                        <tbody>
                            <tr>
                                <th style="width: 30%;">اسم الطبق</th>
                                <td>@Model.Name</td>
                            </tr>
                            <tr>
                                <th>القسم</th>
                                <td>@(Model.Category?.Name ?? "بدون قسم")</td>
                            </tr>
                            <tr>
                                <th>السعر</th>
                                <td>@Model.Price.ToString("C")</td>
                            </tr>
                            <tr>
                                <th>وقت التحضير</th>
                                <td>@Model.PreparationTime دقيقة</td>
                            </tr>
                            <tr>
                                <th>الوصف</th>
                                <td>@(string.IsNullOrWhiteSpace(Model.Description) ? "لا يوجد وصف" : Model.Description)</td>
                            </tr>
                            <tr>
                                <th>حالة التوفر</th>
                                <td>
                                    @if (Model.IsSoldOut)
                                    {
                                        <span class="badge bg-danger">نفذت الكمية</span>
                                    }
                                    else if (Model.IsAvailable)
                                    {
                                        <span class="badge bg-success">متاح للطلب</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">غير متاح حالياً</span>
                                    }
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>